<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
            crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/styles.css">
    </head>
    <body class="p-4 bg-dark text-white">
        <div>
            <h1 class="h1 text-center p-2 mb-3">TaskPad</h1>
            <div class="mb-3">
                <form action="/create" method="post">
                    <div class="form-group">
                        <label class="h4 mb-2"
                            for="exampleFormControlInput1">Title</label>
                        <input class="form-control" type="text"
                            placeholder="Enter your Title here" name="title">
                    </div>
                    <div class="form-group my-4">
                        <label for="exampleFormControlTextarea1"
                            class="form-label h4 mb-2">Task Details</label>
                        <textarea class="form-control"
                            id="exampleFormControlTextarea1"
                            rows="8"
                            placeholder="start writing your text here"
                            name="details"></textarea>
                    </div>

                    <div class="my-4">
                        <button type="submit"
                            class="btn btn-outline-info">Create Task</button>
                    </div>

                </form>

            </div>

            <div class="tasks" style="display:flex; gap:20px; flex-wrap: wrap;">
                <% if(files.length > 0) { %>
                <% files.forEach(function(val){ %>
                <div class="card text-bg-info mb-3"
                    style="min-width: 250px; max-width: 30vw;">
                    <div class="card-header">Task Details</div>
                    <div class="card-body">
                        <h5 class="card-title"><%= val.substring(0,
                            val.lastIndexOf('.')) %></h5>

                        <div class="d-flex justify-content-between pe-3">
                            <a href="/files/<%= val %> "
                                class="card-link"
                                style="color: #0C134F"><small>Read
                                    more</small></a>
                            <a class="card-link "
                                href="/edit/<%= val%>"
                                style="color: #0C134F"><small>rename</small></a>
                        </div>

                    </div>
                </div>
                <%}) } else { %>
                <h3>No Tasks Yet</h3>
                <% } %>
            </div>

        </div>

        <script src="/javascripts/script.js"></script>
    </body>
</html>